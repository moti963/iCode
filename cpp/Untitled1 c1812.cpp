/*C program to compute the sum of the two given integer values. If
the two values are the same, then return triple their sum*/

#include<stdio.h>
int main()
{ int test(int x, int y);
	printf("%d", test(7,8));
	printf("%d",test(4,4));	 
	
	int test(x, y);
	return x==y ? (x+y)*3 :(x+y);
	
	}
