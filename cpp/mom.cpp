#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using namespace std;

int main()
{
    fastio;
    int t,x=1;
    cin >> t;
    while (t--)
    {
        cout <<"Case #"<< x<<": "<< "MAA"<<"\n";
        x++;
    }

    return 0;
}
