#include<bits/stdc++.h>
#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

using namespace std;


int main()
{
    fastio;
    int n,c;
    cin>>n>>c;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
    {
        cin>>v[i];
    }
    int m;
    cin>>m;
    vector<int> x(m);
    vector<int> y(m);
    for (int i = 0; i < m; i++)
    {
        cin>>x[i]>>y[i];
    }

    
    
    

    return 0;
}
//INCOMPLETE
